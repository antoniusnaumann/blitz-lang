// Test file for string built-in methods

fn main() {
	let str = "hello"
	let chars = str.chars()
	let len = chars.len()
	print("String length:")
	print(len)
	
	// Test substring
	let sub = chars.substring(1, 4)
	print("Substring (1, 4):")
	print(sub)
}
