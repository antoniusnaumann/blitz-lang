fn compile_file(path String) ast::Ast {
	// TODO: proper error handling
	let content = read(path).unwrap()

	// CODE -> AST
	var parser = new_parser(content)
	ast::Ast(items: parser.mut.parse(), origin: path)
}

fn compile(path String) {
	todo("check if path or dir and then call the appropriate function")
	let asts = todo("lower individual files")

	// AST -> HIR
	let hir = asts.lower()

	// HIR -> TIR
	todo("HIR -> TIR")

	// TIR -> MIR

	// MIR -> LIR
}
