test "Parse empty func" {
    let defs = "fn foo() {}".parse()
    mut count = 0
    assert defs.len() == 1
    switch defs[0] {
        Fn { count = 1 }
        else { panic("Expected function") }
    }
    assert count == 1
}
