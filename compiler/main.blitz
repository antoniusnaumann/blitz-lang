fn main() {
	let content = read("main.blitz").unwrap()
	print(content)

	mut lexer = new_lexer(content)
	mut token = lexer.mut.next()
	while token != none {
		print(token)
		token = lexer.mut.next()
	}
}
